spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: CLIENT_ID
 mail:
   password: NEED_TO_INPUT / 띄어쓰기 없이 넣기!
custom:
  security:
    oauth2:
      client:
        registration:
          kakao:
            devUser:
              oauthId: oauthId
              nickname: nickname

   // 여기다 형식 만들어서 만들고 나!중!에! secret.yml로 옮겨서 사용!